# backend db , ( ** using docker image => container)
services:
  my-sql:
    image: mysql:latest
    container_name: hotel-managment-system
    ports: 
      - "3306:3306"
    environment:
        MYSQL_ROOT_PASSWORD : "mera-hotel@2506"
        MYSQL_ROOT_USER : "root"

    