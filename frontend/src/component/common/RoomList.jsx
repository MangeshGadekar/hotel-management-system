import React from 'react';
import Rooms from '../../config/Room.json';
import RoomCard from './RoomCard';

const RoomList = () => {
  // Optional: Add favorite toggle handler
  

  return (
    
  );
};

export default RoomList;